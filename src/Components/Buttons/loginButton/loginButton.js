import React from 'react';
import {Button} from 'react-materialize';
import './loginButton.css';

const loginButton = () => <Button waves='light' className='button'>button</Button>;

export default loginButton;